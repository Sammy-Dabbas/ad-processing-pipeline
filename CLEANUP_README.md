# 🧹 **Data Cleanup & Organization**

## ✅ **Ad Event Data (Clean & Organized)**

### **📁 `ad_event_data/` - Production Ad Events**
```
ad_event_data/
├── raw_ad_events.jsonl          # 25MB - Original ad events (24,894 events)
├── realistic_ad_events.jsonl    # 49MB - High-volume realistic events (200K events) 
└── processed_ad_events.jsonl    # 60MB - Processed events with enrichment
```

**Data Quality:**
- ✅ **Event Types**: impression, click, conversion
- ✅ **Schema**: Complete ad tech schema with campaign_id, revenue, etc.
- ✅ **Performance**: Validated at 50K+ events/sec processing
- ✅ **Deduplication**: Zero duplicates in processed data

## ❌ **Wikipedia Data (Legacy - Can be Removed)**

### **📁 `data/` - Old Wikipedia Data (~3GB)**
```
data/
├── raw*.jsonl           # 28 files of Wikipedia RecentChanges (~3GB total)
└── processed.jsonl      # 5.7MB of processed Wikipedia data
```

**Status**: No longer needed for ad event processing system

## 🧪 **Test Data Directories**

### **📁 `test_data/` - Development Testing**
- Can be removed after cleanup

### **📁 `realistic_perf_test/` - Performance Testing** 
- Can be removed after data moved to `ad_event_data/`

### **📁 `1m_per_sec_test/` - Ultra High Performance Testing**
- Generated by our 1M events/sec tests

---

## 🎯 **Recommended Cleanup Actions**

### **1. Remove Wikipedia Data (Free ~3GB)**
```bash
# Remove old Wikipedia data
rm -rf data/raw-*.jsonl
rm data/processed.jsonl

# Keep data/ directory for future ad events
```

### **2. Remove Test Directories**
```bash
# Remove old test directories
rm -rf test_data/
rm -rf realistic_perf_test/
```

### **3. Update Service Paths**
Update all services to use `ad_event_data/` instead of `data/` for ad events:
- ✅ Event generator output
- ✅ Consumer input
- ✅ API data source

---

## 📊 **Current Space Usage**

```
Wikipedia Data:     ~3.0 GB  (can be removed)
Ad Event Data:      ~134 MB  (keep - production data)
Test Directories:   ~60 MB   (can be removed)
Performance Tests:  ~400 MB  (temporary - can be cleaned)
```

**After cleanup: ~134MB vs ~3.5GB** (96% space reduction)

---

## 🚀 **Post-Cleanup Benefits**

1. **Clean Repository**: Only ad event data, no Wikipedia confusion
2. **Faster Git Operations**: 96% smaller repository
3. **Clear Data Flow**: Obvious where ad events are stored
4. **Production Ready**: Clean structure for deployment
5. **Interview Friendly**: Easy to demo without legacy data

**The ad event processing system is now cleanly separated from the original Wikipedia prototype!** ✅
